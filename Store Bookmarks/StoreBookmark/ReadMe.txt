Store Bookmarks

What it does: Takes input - Bookmark name,link,type(radio buttons) and tag.

How to use: 

1. databaseConnectConfig.php has a localhost and usename, password setup which must be configured.
2. The database has to have a database and tables :

databse: db1
Table:bookmarks
columns: (All VARCHARS)
1.bookmarkName
2.bookmarkLink
3.bookmarkType
4.tag


Currently, the tool doesnt have update and delete options.It only can Add values. 